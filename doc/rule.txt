
/*!

\page rule 虚拟机层与系统中间层通信协议

\section 虚拟机层与中间层接口
- 数据保存格式
	-    所有在此虚拟机上执行有关于数据的保存，都是低位在前高位在后\n


\section 数据传输协议
-	虚拟机请求数据格式
	- API_ID(两个字节,对应的在中间层调用的api)+数据总长度(两个字节)+数据(长度+数据)+......
-	中间层返回数据格式
	- 数据长度(两个字节)+数据+数据长度(两个个字节)+数据+.....
	
\section 执行虚拟机指令最终操作账户指令
   -     账户(6个个字节) +对账户操作类型(1个字节)+金额(8字节)
   
\section 中间层对虚拟机输出指令权限校验
    -    检查此交易输出指令加减金额是否平衡
    -    只可以应用账户打钱到用户账户，否则不行

*/

